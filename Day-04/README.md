# SQL Developer Internship – Task 4  
## Sorting and Limiting Data

## 📌 Overview
This task focuses on understanding how to **sort**, **limit**, and **paginate** data in SQL using the `ORDER BY`, `LIMIT`, and `OFFSET` clauses. These operations are essential for building real-world applications such as dashboards, leaderboards, and paginated views.

## 🛠 Tools Used
- MySQL Workbench  
- SQL (MySQL)

## 📂 Table Used
The task was performed on a table containing the following columns:
- `ID`
- `NAME`
- `EMAIL`
- `AGE`

## ✅ Operations Performed
- Sorting data in **ascending and descending order**
- Sorting using **multiple columns**
- Limiting the number of results using `LIMIT`
- Combining `ORDER BY` with `WHERE`
- Implementing **pagination** using `OFFSET`
- Creating a **leaderboard-style query**
- Testing **edge cases** such as empty result sets and NULL values
- Sorting using **column aliases**

