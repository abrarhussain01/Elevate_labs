📘 SQL Subqueries Practice

This project demonstrates practical usage of SQL subqueries using an employees table containing employee salary and department information.

🧩 Topics Covered

* Subqueries in WHERE, FROM, and SELECT clauses

* Finding employees earning more than average salary

* Comparing subquery-based queries vs JOIN-based queries

* Implementing correlated subqueries

* Understanding execution flow of nested queries

* Identifying scenarios where subqueries are unavoidable

* Debugging common subquery errors (single-row vs multi-row issues)
