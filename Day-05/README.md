# SQL Developer Internship – Task 5  
## Aggregate Functions & GROUP BY

## 📌 Task Overview
This task focuses on understanding and applying **SQL aggregate functions** along with the **GROUP BY** and **HAVING** clauses. The goal is to analyze tabular data statistically using real-world SQL queries.

## 🛠 Tools Used
- MySQL Workbench  
- SQL (MySQL syntax)

## 📂 Dataset Description
The dataset contains student information with the following columns:
- `id` – Unique identifier
- `name` – Student name
- `email` – Student email address
- `age` – Student age

## 📊 Concepts Covered
- COUNT, SUM, AVG, MIN, MAX
- GROUP BY clause
- HAVING clause
- Difference between WHERE and HAVING
- Handling NULL values in aggregate functions
- Real-world data aggregation examples



